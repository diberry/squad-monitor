// Decision feed formatter - renders decision list

import { Decision } from '../../core/types';

export class DecisionFeedFormatter {
  format(decisions: Decision[]): string {
    // Format decisions into ANSI list
    return '';
  }
}

// Agent board formatter - renders agent status table

import { AgentStatus } from '../../core/types';

export class AgentBoardFormatter {
  format(agents: AgentStatus[]): string {
    // Format agents into ANSI table
    return '';
  }
}
